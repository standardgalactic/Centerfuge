# yarncrawler

Blender still-image generator (no animations). Generates `.blend` and `.png` per scene.

## Usage
```bash
python3 yarncrawler_blender_gen.py --outdir out --scenes all --render
```
Requires Blender to be installed (`blender` on PATH) for `--render`. Scripts can be generated without Blender.
